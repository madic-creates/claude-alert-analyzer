apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: monitoring
resources:
  - rbac.yaml
  - deployment.yaml
  - service.yaml
  - history-pvc.yaml
  # Copy secret.example.yaml to secret.yaml, fill in values, then uncomment:
  # - secret.yaml
